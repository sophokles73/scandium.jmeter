# number of LWM2M clients to start
threads=20

# number of APPLICATION messages to send perr client
loops=10000

# the following property instructs JMeter to output results in XML format
# this is required if using the Jenkins Performance plugin because it does not yet
# (as of 07.2015) understand JMeter's new CSV format
jmeter.save.saveservice.output_format=xml